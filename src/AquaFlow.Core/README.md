# AquaFlow.Core

Доменная модель сети труб и детерминированный симулятор (`IPipeSimulator`).
Реализация появится в M1. Проект намеренно не зависит ни от UI, ни от ML —
это фундамент, который должен быть тестируемым и переиспользуемым.
